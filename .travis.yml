dist: xenial
language: python
python:
  - "3.6"
install:
  - pip install pipenv
  - pipenv install --dev 
  - pipenv install codecov
script:
  - python3 -m pytest --cov=metrontagger tests/
after_success:
  - codecov
